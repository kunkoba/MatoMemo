# Dockerイメージの作成する
docker build -f dev/Dockerfile.dev -t matomemo-backend-dev .

# コンテナを起動する
docker run -p 5000:5000 -v ./src/backend:/app matomemo-backend-dev


# 実行コマンド
# ターミナルでルートフォルダ（docker-compose.yml がある場所）に移動し、以下を叩いてください。
cd C:\Users\Owner\Desktop\__Developer\MatoMemo\_src
docker-compose up -d --build
docker compose up -d --build api
docker compose up -d --build web
docker compose up -d --build db
docker compose up -d --build --no-deps api
